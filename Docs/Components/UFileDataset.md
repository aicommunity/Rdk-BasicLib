# UFileDataset

## RU

### Назначение

**Класс**: `UFileDataset` — набор файловых датасетов для матричных источников

**Регистрация**: `UBCLLibrary.cpp` / `UIOLibrary.cpp` → `UploadClass("UFileDataset", ...)`.
См. также: [UMatrixSourceFileFamily](UMatrixSourceFileFamily.md)


### Свойства и методы

См. заголовок `Libraries/Rdk-BasicLib/Core/UFileDataset.h` и [API-Overview.md](../API-Overview.md).

---

## EN

### Purpose

**Class**: `UFileDataset` — file dataset container for matrix sources

**Registration**: `UploadClass("UFileDataset", ...)` in Rdk-BasicLib.

See `Core/UFileDataset.h` and [API-Overview.md](../API-Overview.md).
