# UFileDataset

**Каталог:** [Component-Catalog.md](../Component-Catalog.md).

## RU

### Назначение

**Класс**: `UFileDataset` — набор файловых датасетов для матричных источников (поверх `UMatrixSourceFileSep`).

**Регистрация**: `UBCLLibrary.cpp` → `UploadClass("UFileDataset", ...)`.

См. также: [UMatrixSourceFileFamily](UMatrixSourceFileFamily.md#ufiledataset), [UMatrixSourceFileSep](UMatrixSourceFileSep.md).

### Свойства и методы

Ключевые свойства (см. UML в семейном обзоре): `NumSamples`, `CurrentSample`, `SampleData`. Полные таблицы — в заголовке `Core/UFileDataset.h` и [API-Overview.md](../API-Overview.md).

---

## EN

### Purpose

**Class**: `UFileDataset` — file dataset container for matrix sources (on top of `UMatrixSourceFileSep`).

**Registration**: `UploadClass("UFileDataset", ...)` in Rdk-BasicLib.

See also: [UMatrixSourceFileFamily](UMatrixSourceFileFamily.md#ufiledataset), [UMatrixSourceFileSep](UMatrixSourceFileSep.md).

### Properties and methods

Key properties: `NumSamples`, `CurrentSample`, `SampleData`. Full tables in `Core/UFileDataset.h` and [API-Overview.md](../API-Overview.md).
