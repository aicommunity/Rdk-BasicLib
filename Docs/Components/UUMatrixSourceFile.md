# UUMatrixSourceFile

## RU

### Назначение

**Класс**: `UUMatrixSourceFile` — вариант файлового источника матрицы

**Регистрация**: `UBCLLibrary.cpp` / `UIOLibrary.cpp` → `UploadClass("UUMatrixSourceFile", ...)`.
См. также: [UMatrixSourceFileFamily](UMatrixSourceFileFamily.md)


### Свойства и методы

См. заголовок `Libraries/Rdk-BasicLib/Core/UUMatrixSourceFile.h` и [API-Overview.md](../API-Overview.md).

---

## EN

### Purpose

**Class**: `UUMatrixSourceFile` — matrix file source variant

**Registration**: `UploadClass("UUMatrixSourceFile", ...)` in Rdk-BasicLib.

See `Core/UUMatrixSourceFile.h` and [API-Overview.md](../API-Overview.md).
